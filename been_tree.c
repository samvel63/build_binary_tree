#include <stdlib.h>
#include <stdio.h>

struct bin_tree {
    int data;
    struct bin_tree * right, * left;
};
typedef struct bin_tree node;

int main(void)
{
    int *list_parent_vertex;
    node *root;

    return 0;
}
